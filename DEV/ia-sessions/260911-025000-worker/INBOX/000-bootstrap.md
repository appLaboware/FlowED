# INBOX — WRK-001

Initial orientation: read `DEV/ia-sessions/COORDINATION.md` and this session's `CONTEXT.md`. Then inspect the existing CCP/materialization drafts in `docs/pt-br/evolution/` and propose the smallest executable vertical slice before writing production code.

Do not wait for chat copy/paste. Use the repository as the handoff source.
